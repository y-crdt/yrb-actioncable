{
  "name": "yrb",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "scripts": {
    "release": "turbo run build lint test && changeset version && changeset publish"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "engines": {
    "node": ">= 16",
    "yarn": ">= 1"
  }
}
