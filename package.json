{
  "name": "yrb",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "scripts": {
    "release": "turbo run build lint test && changeset version && changeset publish"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.27.7"
  },
  "engines": {
    "node": ">= 16",
    "yarn": ">= 1"
  }
}
