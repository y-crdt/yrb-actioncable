{
  "name": "yrb",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "release": "turbo run build lint test && changeset version && changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.26.0"
  }
}
